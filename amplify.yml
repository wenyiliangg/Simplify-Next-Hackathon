version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static frontend requires no build step"
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths: []
